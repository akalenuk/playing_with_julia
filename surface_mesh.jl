module SurfaceMesh
  export build_3d_mesh, distance_to_gradient_operator
  function build_3d_mesh(bbox_min, bbox_max, cube_size, distance_in_point, gradient_in_point)
  end

  function distace_to_gradient_operator(distance_in_point)
  end

  function tests()
  end
end
